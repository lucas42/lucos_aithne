# Compiled binaries
lucos_aithne

# Go test cache
*.test
coverage.out

# Environment files
.env

# Canonical scope vocabulary — generated by scripts/fetch-scopes.sh (or go generate).
# Not committed: the Dockerfile fetches canonical from lucos_auth_scopes at build time;
# run scripts/fetch-scopes.sh once for local go build / go test.
/scopes.yaml
